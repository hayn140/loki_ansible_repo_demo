---

- name: testing using defaults and files
  hosts: dev-la
  become: true
  tasks:

  - name: Create groups in defaults variable file
    ansible.builtin.group:
      name: '{{ item }}'
      state: present
    loop:
      - aloha
      - hawaii
